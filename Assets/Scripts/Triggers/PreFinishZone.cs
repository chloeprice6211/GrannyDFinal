using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PreFinishZone : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
        //if (other.CompareTag("Car"))
        //{
        //    UIManager.Instance.fadeAnimation.Play(UIManager.Instance.endFadeClip.name);
        //}
    }
}
